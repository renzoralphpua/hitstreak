// lib/decks/rules/one-piece.ts — Task 3 replaces this stub with the real One Piece rules
// (1 Leader + 50 main; ≤4 per card number; every main-deck card shares a colour with the Leader).
import type { ValidationResult } from "../types";

// Task 3
export function validateOnePiece(): ValidationResult {
  return { valid: true, errors: [] };
}

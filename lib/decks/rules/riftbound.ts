// lib/decks/rules/riftbound.ts — Task 4 replaces this stub with the real Riftbound rules
// (1 Legend; 1 Chosen Champion; exactly 40 main; ≤3 per name; Signatures; domains; 12 runes; 3 Battlefields).
import type { ValidationResult } from "../types";

// Task 4
export function validateRiftbound(): ValidationResult {
  return { valid: true, errors: [] };
}
